import config from "../../config";
import { isDev } from "../../utils/environmentHelper";

export const apiUrl = 'https://trash2cashapi.trash2cash.us/v1/api/'

// isDev ? "https://localhost:7189/api/" : config.apiUrl;
